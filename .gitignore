node_modules/
dist/
.env
.env.local
*.log

# Bundle generado por el helper de Claude Design (editor completo + contenido
# inline, ~2MB). No es código fuente: se re-genera desde design/*.dc.html +
# design/canvas.json. Ver CLAUDE.md.
design/crm-ventas-dashboard-direcciones.html

# Temporales del CLI de Supabase (npx supabase ...)
supabase/.temp/

# Overrides locales de Claude Code (nunca se commitean)
.claude/settings.local.json
